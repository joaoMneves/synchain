# Sync Chain


```
Account
    Dir/Global (synced by all devices)
        /account 
            /devices (public keys, permissions)
            /identity (user id, account keypair)
    Dir/proto 
     ex:
        /drive
        /protocols (ex: rss) 
        /confs (ex: dark mode)
        /app.id (app specific data)
```
